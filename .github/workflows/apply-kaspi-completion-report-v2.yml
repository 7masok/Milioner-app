name: Run Kaspi archived completion report patch
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-kaspi-completion-report-v2.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Patch
        run: |
          python .github/scripts/use_kaspi_archive_report.py
          node --check cloudflare/millioner-api/src/index.js
      - name: Commit
        run: |
          git config user.name millioner-maintenance
          git config user.email actions@users.noreply.github.com
          git add cloudflare/millioner-api/src/index.js
          git diff --cached --quiet && exit 0
          git commit -m 'Use archived Kaspi orders for completion report'
          git push origin HEAD:main
