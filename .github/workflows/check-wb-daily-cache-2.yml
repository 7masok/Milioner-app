name: Check populated WB daily cache
on:
  push:
    branches: [main]
    paths: ['.github/workflows/check-wb-daily-cache-2.yml']
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS 'https://millioner-api.7masok.workers.dev/api/wb-dashboard-buyouts?market=WB&days=-1'
