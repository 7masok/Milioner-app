name: Refresh WB realized sales now
on:
  push:
    paths: ['.github/workflows/wb-refresh-now.yml']
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: WB1 direct KZT raw
        run: |
          curl -sS -D /tmp/h 'https://millioner-api.7masok.workers.dev/api/wb-sales-direct?market=WB&days=1' -o /tmp/b || true
          cat /tmp/h
          echo BODY
          cat /tmp/b
