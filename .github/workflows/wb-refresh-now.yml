name: Refresh WB realized sales now
on:
  push:
    paths: ['.github/workflows/wb-refresh-now.yml']
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: WB1 sold status history raw
        run: |
          curl -sS -D /tmp/h 'https://millioner-api.7masok.workers.dev/api/wb-sold-history-debug?market=WB' -o /tmp/b || true
          cat /tmp/h
          echo BODY
          cat /tmp/b
