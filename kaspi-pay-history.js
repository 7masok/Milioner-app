(function(){function history(){const a=[...(state.kaspiPayImports||[])].sort((x,y)=>(y.importedAt||0)-(x.importedAt||0));const body=a.length?a.map(x=>'<div class="item" style="margin-top:8px"><b>'+esc(x.fileName||'Kaspi Pay')+'</b><div class="muted">'+esc(x.fromDate||'—')+' — '+esc(x.toDate||'—')+' · '+Number(x.rowsCount||0)+' строк</div></div>').join(''):'<div class="empty">Импортов Kaspi Pay пока нет</div>';showSheet('<h3>Kaspi Pay · история</h3>'+body)}window.showKaspiPayHistory=history;})();
