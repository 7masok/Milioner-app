(function(){const K=window.KaspiPay;if(!K)return;const base=marketplaceProductStats;marketplaceProductStats=function(market,days=reportPeriod){if(market!=='Kaspi')return base(market,days);const pay=K.breakdown(days);if(!pay.covered||!pay.rows)return base(market,days);const old=base(market,days),m=new Map(old.filter(x=>x.productId).map(x=>[String(x.productId),x])),ads=kaspiAdsBreakdown(days),out=[];for(const[pid,p]of pay.byProduct){const prev=m.get(String(pid))||{},oq=Math.max(0,Number(prev.qty)||0),nq=Math.max(0,Number(p.qty)||0),cost=Math.max(0,Number(prev.cost)||0)*(oq?Math.min(1,nq/oq):1),ad=Math.max(0,Number(ads.byProduct.get(String(pid)))||0),fees=Number(p.financialExpenses||0);out.push({productId:pid,name:p.name,qty:p.qty,revenue:p.revenue,cost,commission:p.commission,cardCommission:p.cardCommission,kaspiPay:p.kaspiPay,delivery:p.delivery,otherFees:Number(p.paymentGuarantee||0)+Number(p.kaspiTravel||0)+Number(p.bonusProduct||0)+Number(p.bonusReview||0),fees,ads:ad,profit:Number(p.revenue||0)-cost-fees-ad,financialActual:true})}return out.sort((a,b)=>b.profit-a.profit)};})();
